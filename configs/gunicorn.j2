[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=sammy
Group=www-data
WorkingDirectory={{ app_folder }}
ExecStart={{virtualenv_dir}}/bin/gunicorn --access-logfile - --workers 3 --bind 127.0.0.1:8000 MyApp.wsgi:application

[Install]
WantedBy=multi-user.target
