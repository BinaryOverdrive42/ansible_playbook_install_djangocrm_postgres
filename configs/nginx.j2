server {
	listen 80;
	server_name localhost;
	charset utf-8;


	location /static {
		alias "{{ app_folder }}/static";
	}

	location / {
		include proxy_params;
		proxy_pass http://unix:{{ app_folder }}/{{ app_name }}.sock;
	}
}
